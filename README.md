# oklab-palette

Generates color palettes in the OKLab color space. `oklab_gen.py` produces
swatches (e.g. 20 colors with lightness×chroma held constant) with full
OKLab→sRGB conversion; `index.html` is a rendered sample on a black background.

## Usage
```bash
python3 oklab_gen.py
```
